// Sprint 2: Clack select from installed + confirm, rm -rf install_path,
// remove telemetry block from SKILL.md (if file survives), delete from local
// SQLite, DELETE /installations/:id.

export async function removeCommand(_opts: {
  skill?: string;
  purgeStats?: boolean;
}): Promise<void> {
  console.log('TODO: remove');
}
