// Sprint 1: Clack select of devices + confirm (skip with --force).

export async function authRevokeCommand(_opts: {
  device?: string;
  force?: boolean;
}): Promise<void> {
  console.log('TODO: auth revoke');
}
