// Sprint 2: validate SKILL.md + frontmatter.name, respect .skillzignore /
// .gitignore, pack tarball (max 10 MB), sha256, POST /skills/:name/versions,
// Clack spinner during upload.

export async function pushCommand(_opts: { path: string }): Promise<void> {
  console.log('TODO: push');
}
