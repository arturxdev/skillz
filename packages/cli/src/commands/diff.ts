// Sprint 4: fetch v1 + v2 tarballs, extract, diff SKILL.md.

export async function diffCommand(_opts: {
  skill: string;
  v1: number;
  v2: number;
}): Promise<void> {
  console.log('TODO: diff');
}
