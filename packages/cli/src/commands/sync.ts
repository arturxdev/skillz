// Sprint 3: read pending_pings from local SQLite (batch 100), POST /track/batch,
// if 2xx delete; if 401 print a warning to re-link.

export async function syncCommand(): Promise<void> {
  console.log('TODO: sync');
}
