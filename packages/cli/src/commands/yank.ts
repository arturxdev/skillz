// Sprint 2: parse skill@version if given, else Clack select skill → select version
// → text reason → confirm. POST /skills/:name/versions/:v/yank.

export async function yankCommand(_opts: { target?: string }): Promise<void> {
  console.log('TODO: yank');
}
